meta:
  environment: node
  toolchain:
    name: node
    version: 18

build:
  additionalCommands: npm install -g npm@11.6.2 && npm ci && npm run build && ls -la dist

run:
  command: npx serve -s dist -l $PORT
  containerPort: 0