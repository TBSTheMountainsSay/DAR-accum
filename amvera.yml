meta:
  environment: node
  toolchain:
    name: node
    version: 18

build:
  command: npm install && npm run build
  outputDir: dist

run:
  command: serve
  args: -s dist -l 3000
  containerPort: 3000
