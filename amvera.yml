meta:
  environment: node
  toolchain:
    name: node
    version: 18

build:
  additionalCommands: npm install && npm run build
run:
  scriptName: ""
  scriptArguments: serve -s dist -l 3000
  command: npx serve -s dist -l 3000
  containerPort: 3000
